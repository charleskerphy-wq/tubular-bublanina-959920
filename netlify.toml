[build]
  command = "vite build"
  publish = "dist/client"
  functions = "netlify/functions"
[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888

[images]
  remote_images = ["https://images.unsplash.com/.*"]
